<div class="container">
  <div class="row justify-content-center mt-3 mb-5">
    <div class="col-xl-10">
      <div class="card">
        <div class="card-body">
          <div id ='messages' data-secret-id='<%= current_user.secret_patient_id %>' data-current-user-id='<%= current_user.id %>' >
            <%= render @messages %>
          </div>
        </div>
      </div>
      <div class="card p-3 pt-1 fixed-form">
        <form id="new_message" class="mt-1">
          <div class="form-group d-flex">
            <textarea id="message_content" data-behavior="room_speaker" class="form-control me-2 flex-grow-1" rows="2"></textarea>
            <input type="submit" value="送信" class="submit-button">
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
