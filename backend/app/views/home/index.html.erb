<h1>ようこそ</h1>
<p>トップページです。</p>

<% if user_signed_in? %>
  <p>Welcome, <%= current_user.name %>!</p>
<% else %>
  <p>Please sign in.</p>
<% end %>

<%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>

<% if user_signed_in? %>
  <p>あなたの Secret Patient ID: <%= @secret_patient_id %></p>
<% else %>
  <p>You are not signed in.</p>
<% end %>
